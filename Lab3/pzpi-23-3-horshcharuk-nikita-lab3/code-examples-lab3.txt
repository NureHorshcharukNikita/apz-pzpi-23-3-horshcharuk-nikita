Назва інструменту та версія:
ChatGPT (OpenAI), модель GPT-5.3.

Мета використання:
Інструмент використано для формування текстового наповнення розділів:

3.1 Загальні відомості про веб-застосунок Elevate
3.2 Інженерні рішення
3.3 Основні компоненти веб-застосунку
3.4 Взаємодія компонентів веб-застосунку
3.5 Технології реалізації
3.6 Взаємодія веб-застосунку з системою Elevate
3.7 Структура проєкту веб-застосунку
3.8 UML-діаграми веб-застосунку

а також для допомоги у написанні та структуризації програмного коду веб-застосунку.

Штучний інтелект застосовувався для:

структуризації опису веб-застосунку Elevate;
деталізації архітектури front-end частини;
формування опису компонентів системи та їх взаємодії;
пояснення використаних технологій (React, TypeScript, Vite);
генерації прикладів програмного коду (React-компоненти, API-запити, авторизація);
оптимізації структури проєкту та повторного використання коду.

Внесок автора:
Автор сформулював вихідні запити з описом архітектури веб-застосунку Elevate, логіки роботи компонентів, структури проєкту та вимог до реалізації функціональності. Отримані результати були перевірені на відповідність реалізованому програмному коду.

Згенеровані фрагменти коду були адаптовані до конкретного проєкту, інтегровані у структуру застосунку, доповнені обробкою помилок, типізацією та логікою взаємодії з сервером. Автор також виконав рефакторинг коду, забезпечив узгодженість між компонентами та оптимізував структуру застосунку.

Згенерований текст було відредаговано, доповнено технічними деталями, уточнено термінологію, а також структуровано відповідно до вимог академічного звіту.

Використані запити:
1. Запит для пункту 3.1 Загальні відомості про веб-застосунок Elevate

Запит до ШІ:
"Опиши веб-застосунок Elevate як частину системи гейміфікації з веб-клієнтом, сервером, мобільним застосунком та IoT-компонентом."

Результат:
Веб-застосунок описано як клієнтську частину системи, що взаємодіє із сервером через REST API, використовує JWT-автентифікацію та забезпечує роботу користувацького і адміністративного інтерфейсів.

2. Запит для пункту 3.2 Інженерні рішення

Запит до ШІ:
"Опиши інженерні рішення для React SPA з TypeScript, маршрутизацією, API та локалізацією."

Результат:
Сформовано опис SPA-архітектури, компонентного підходу, використання React Router, Axios та i18next для локалізації.

3. Запит для пункту 3.3 Основні компоненти

Запит до ШІ:
"Опиши основні компоненти веб-застосунку Elevate (Auth, User UI, Admin UI, API, routing, state management)."

Результат:
Описано ключові модулі системи, їх призначення та взаємодію.

4. Запит для пункту 3.4 Взаємодія компонентів

Запит до ШІ:
"Опиши взаємодію компонентів React-застосунку з сервером через JWT і REST API."

Результат:
Пояснено процес авторизації, передачу токена, використання Axios і взаємодію між компонентами.

5. Запит для пункту 3.5 Технології

Запит до ШІ:
"Опиши стек технологій веб-застосунку Elevate."

Результат:
Сформовано опис React, TypeScript, Vite, Axios, i18next, ESLint та серверних технологій.

6. Запит для пункту 3.6 Взаємодія з системою

Запит до ШІ:
"Опиши як веб-клієнт взаємодіє з сервером, мобільним застосунком і IoT."

Результат:
Описано централізовану взаємодію через сервер і REST API.

7. Запит для пункту 3.7 Структура проєкту

Запит до ШІ:
"Опиши структуру React-проєкту з модулями api, auth, components, pages, hooks."

Результат:
Сформовано опис модульної структури проєкту.

8. Запит для пункту 3.8 UML-діаграми

Запит до ШІ:
"Опиши UML-діаграми для веб-застосунку Elevate."

Результат:
Наведено опис use case, component, activity та package діаграм.

Запити, пов’язані з програмним кодом
9. Запит для реалізації авторизації

Запит до ШІ:
"Напиши приклад авторизації в React з використанням JWT і Axios."

Результат:
Було згенеровано код компонента авторизації, який виконує запит до API, отримує JWT-токен, зберігає його у localStorage та забезпечує подальшу автентифікацію запитів.

10. Запит для HTTP-клієнта

Запит до ШІ:
"Напиши Axios instance з автоматичним додаванням JWT токена."

Результат:
Було створено централізований HTTP-клієнт із interceptors для додавання токена до заголовків кожного запиту.

11. Запит для захисту маршрутів

Запит до ШІ:
"Реалізуй protected route в React для перевірки ролі користувача."

Результат:
Згенеровано компонент захищених маршрутів із перевіркою наявності токена та ролі користувача.

12. Запит для структури компонентів

Запит до ШІ:
"Покажи приклад структури компонентів для React проєкту з поділом на pages і components."

Результат:
Було запропоновано структуру проєкту з розділенням на логічні модулі та приклади базових компонентів.

13. Запит для локалізації

Запит до ШІ:
"Покажи як реалізувати i18next у React з підтримкою двох мов."

Результат:
Було згенеровано приклад конфігурації i18next та використання перекладів у компонентах.